# ============================================
# core/prompts/templates/tools.py - 工具调用提示词
# ============================================

TOOL_FORMAT_PROMPT = """你可以调用以下工具来完成任务：

{tools}

调用工具后，你需要等待工具返回结果，然后基于结果继续推理。
工具返回结果可能被截断（截断处含标记与原长度）；遇到截断时，可缩小查询范围后再次调用以获取完整信息。
如果需要多次调用工具，按需重复这个过程。
当获得足够信息后，用自然语言回答用户。
"""
