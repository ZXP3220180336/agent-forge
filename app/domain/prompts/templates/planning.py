# ============================================
# domain/prompts/templates/planning.py - 规划提示词
# ============================================
"""
规划提示词（PlannerStrategy 三阶段：规划 / 重规划 / 汇总）

工业参照（planner_benchmark 对标）：
- Plan-and-Execute：规划器产出目标式依赖步骤，执行器逐"步"（被步骤约束的 agent 循环）
- 结构化规划优先：计划经 JSON Schema（PLAN_SCHEMA 等）产出，弃文本解析
- introspection 工具目录：规划器知道有哪些工具（文本目录，可指名）但**不调用**
  （generate_structured 恒不传 tools → 无真 tool_calls；步骤 description 写工具动作
  是正常计划语义，非噪音）

规划器只产出计划，不执行；工具调用权只在每步执行器（ReActStrategy）。
"""

PLANNING_PROMPT = """你是任务规划器。把用户目标拆成可交给单次工具执行循环独立完成的步骤序列。

用户目标：{goal}

可用工具目录（规划时可指名使用，但你不直接调用，由执行器执行）：
{tool_descriptions}

规划职责与约束：
1. 步骤自包含：每步只做一件事——写明【目的 + 建议使用的工具与查询/动作目标（若有）+ 预期产出形态 + 可判定完成标准】；单次工具执行循环能独立跑完，不得要求本步内再拆
2. depends_on：每步列出执行前必须已完成的前置步骤编号（指本列表中更靠前的步骤，无则填 []）；列表顺序 = 推荐执行顺序
3. 粒度 2~5 步为宜：过粗（一步塞多个独立查询）或过碎（多步可合并）都要避免；不得重复已完成工作或查询已知信息
4. 只输出符合给定 JSON Schema 的计划对象，不输出任何额外文字或调用意图
"""

REPLAN_PROMPT = """你是任务规划器。某一步执行失败，请重规划「当前计划尚未执行的剩余部分」。

用户目标：{goal}

可用工具目录（规划时可指名使用，但不直接调用）：
{tool_descriptions}

已完成步骤（编号 + 结果摘要；新计划必须基于它们继续，不得重复已完成工作）：
{executed}

失败步骤：{failed_step}
失败原因：{error}

重规划职责：
1. 针对失败根因修订 / 拆分 / 替代该失败步骤；可重排 / 合并尚未执行的后续步骤
2. 新步骤同样自包含；depends_on 只引用上方「已完成步骤编号」或本列表中更靠前的自己（填未知编号会被丢弃）
3. 只输出符合给定 JSON Schema 的 REPLAN_SCHEMA（reason + steps），不输出任何额外文字或调用意图
"""

SUMMARIZE_PROMPT = """你是报告撰写者。基于已执行的步骤结果，产出符合证据链报告规范的结构化结论。

用户目标：{goal}

各步骤执行结果（步骤号 + 产出 + 成败）：
{step_results}

汇总职责（对齐证据链报告，产品主链路消费）：
1. conclusions[].claim 必须可回溯到步骤记录——supporting_evidence 引用真实工具名 + 查询参数（若有）
2. 不编造步骤未返回的数据；证据不足写进 explicit_abstention（无放弃则空数组）
3. confidence 分级：强 / 多来源支撑才给高置信度；失败步骤不得作为证据
4. 明确 assumptions；next_steps 给可执行的具体查询或行动
5. 只输出符合给定 JSON Schema 的报告对象，不输出任何额外文字或调用意图
"""
